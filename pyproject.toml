[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# 强制指定目录，Hatchling 不会自动识别大小写不一致的目录
packages = ["ReDate"]

[project]
name = "redate"
version = "0.2.0"
# 注意：依赖管理已移交给 pixi.toml，本文件仅为了可编辑安装存在。